services:
  k6:
    image: grafana/k6:latest
    container_name: k6
    ports:
      - "5665:5665"
    environment:
      K6_WEB_DASHBOARD: "true"
      BASE_URL: ${BASE_URL}
    volumes:
      - ./dist:/scripts
      - ./results:/results
    working_dir: /scripts
    command: run main.js